# PostgreSQL
POSTGRES_USER=transfer_atlas
POSTGRES_PASSWORD=change_me_in_production
POSTGRES_DB=transfer_atlas

# Database URL (constructed from above — used by the API service)
DATABASE_URL=postgresql://transfer_atlas:change_me_in_production@db:5432/transfer_atlas

# API
API_HOST=0.0.0.0
API_PORT=8000
